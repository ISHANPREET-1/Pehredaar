#!/usr/bin/env bash
# Fallback deploy path for the 2016 Intel Mac this project is built on: if
# `sam build` ever asks for a container, zip src/ and the site-packages
# directory by hand instead of building one. See CLAUDE.md section 3.
# Built in Phase 4.
